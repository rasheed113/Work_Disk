import type { PropsWithChildren } from 'react'
import { Surface } from '../foundation/Surface'
export function Card({children}:{children:React.ReactNode}){return <Surface className="wd-card">{children}</Surface>}
