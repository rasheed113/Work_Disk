# BOT-11 — Sort By Tool Bot

Reusable multi-caller/domain-neutral ordering tool.

Initial sort fields: Number, Size, Name.

Initial directions: Ascending, Descending.

BOT-11 changes ordering of a derived result view only. It does not mutate source records and does not create authority.
