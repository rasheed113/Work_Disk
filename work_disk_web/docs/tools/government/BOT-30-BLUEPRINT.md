# BOT-30 Blueprint

Authorised caller → request validation → DocumentRecordsProvider → record result.

Operations: CREATE, UPDATE, ARCHIVE, RESTORE.

Provider owns persistence, version/conflict handling, retention, evidence integrity and domain-specific policy. BOT-30 remains reusable across government, enterprise, profile, case and records surfaces.